## Run main

To run the main function, use the following command in your terminal:

```bash
mvn exec:java
```

## Run tests
To run the tests, use the following command in your terminal:

```bash
mvn test
```

To run specific tests, use the following command in your terminal:

```bash
mvn -Dtest=MainTest test
```